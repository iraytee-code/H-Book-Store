// import axios from "axios"

const config = {
    // base Url for endpoints
    baseURL: "https://book-store-apis.herokuapp.com/api/v1", 
    httpTimeout: 1000 * 60 * 100, 
  };
  export default config;