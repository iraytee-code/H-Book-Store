import React, { Fragment } from "react";
import { HeroSection, NavBar } from "../components";
const Home = () => {
  return (
    <Fragment>
      <NavBar />
      <HeroSection />
    </Fragment>
  );
};

export default Home;
